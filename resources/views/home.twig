{% extends "layout" %}

{% block extraHead %}
    <script type="application/ld+json">
{
   "@context": "http://schema.org",
   "@type": "WebSite",
   "url": "{{ url('/') }}",
   "potentialAction": {
     "@type": "SearchAction",
     "target": "{{ url('/kereses/search?textToSearch={search_term_string}')}}",
     "query-input": "required name=search_term_string"
   }
}
</script>
{% endblock %}

{% block pageHeader %}
    <div class="page-header">
        <h1>(Régi.)Szentírás.hu <small><span class="hidden-xs hidden-sm">Bibliafordítások az interneten <b>2012 óta</b></span></small></h1>
        <h2>FRISSÍTÉS 2025. 01. 30-án</h2>
        <p>A <code>szentiras.hu</code> címen 2025. januárjától egy új, zárt forráskódú rendszer fut, de <b>ez a rendszer is elérhető marad, és fejlődni fog</b>, 
        továbbra is bátran lehet használni a hivatkozásokat (a <code>regi.szentiras.hu</code> címet használva egyelőre), az API-t, stb.
        <p>Bővebben <a href="/info/review2025"><b>ITT</b></a> tudsz olvasni a frissítési folyamatról.
    </div>
{%  endblock %}

{% block content %}
    {% for row in cathBibles %}
        <h4><a href='/{{ row.abbrev }}'>{{ row.name }} ({{ row.denom }})</a></h4>
        <p class="hidden-xs">{{ row.copyright|raw }}</p>
    {% endfor %}

<br />

{% include 'lectures' with { 'lectures': lectures } %}

<h3><a href='/forditasok'>További fordítások</a></h3>
    {% for row in otherBibles %}
        <h4><a href='/{{ row.abbrev }}'>{{ row.name }} ({{ row.denom }})</a></h4>
    {% endfor %}
{% endblock %}

{% block abbrevlist %}
    {% include 'bookAbbrevList' %}
{% endblock %}